# Adventurer Oliver

Adventurer Oliver is a Python CLI application for recommending adventure games.

## Development

Run the application locally:

```powershell
python -m adventurer_oliver "cozy fantasy adventure for two players"
```

Run project checks:

```powershell
.\scripts\check.ps1
```
